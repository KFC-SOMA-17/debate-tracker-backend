/**
 * Test fixture generators.
 * Each generator is a {@code @Component} that receives the relevant repository
 * and exposes {@code generate(...)} factory methods to persist test data.
 */
package com.debatetracker.debate.fixture;

// Fixture 구현 완료 시, 해당 파일 삭제 예정
