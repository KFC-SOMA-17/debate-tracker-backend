management:
  server:
    port: 8083

  prometheus:
    metrics:
      export:
        enabled: true

  endpoints:
    web:
      exposure:
        include: health,prometheus,metrics,info,loggers,threaddump
      base-path: /monitoring

  endpoint:
    health:
      show-details: always
      show-components: always
      probes:
        enabled: true

    loggers:
      access: unrestricted

    threaddump:
      access: unrestricted

  metrics:
    tags:
      application: ${spring.application.name}
      environment: ${spring.profiles.active:unknown}

    enable:
      jvm: true
      process: true
      system: true
      tomcat: true
      jdbc: true
      hikaricp: true

  health:
    redis:
      enabled: true

  info:
    java:
      enabled: true
    os:
      enabled: true

server:
  tomcat:
    mbeanregistry:
      enabled: true
