# Ralph Progress Log — ralph/websocket-stomp

## Codebase Patterns
- 빌드/검증: macOS 환경이므로 `./gradlew` 사용. typecheck = `./gradlew :app-debate:compileJava`, 테스트 = `./gradlew :app-debate:test`.
- 컨벤션: 루트 CLAUDE.md + CLAUDE.local.md 우선. final 금지, DTO는 record + 보조 생성자(정팩메 회피), 예외는 DebateTrackerException + ErrorCode(던지기 전 log.error), 삼항연산자 금지(Optional), 타입 레벨 주석 금지(TODO만 예외).
- 테스트: @Nested 클래스 + 한글 메서드명(@DisplayName 금지), 검증 2개 이상이면 assertAll로 묶기.
- STOMP 통합 테스트: docs/websocket-stomp-convention.md의 BaseStompTest 패턴(SockJS + WebSocketStompClient, CompletableFuture + 타임아웃)을 따른다.
- 메시지 페이로드 WebSocketMessage(debateId/type/data)와 MessageType enum은 그대로 유지 — transport/destination만 교체.
- debateId는 REST POST /api/debates로 이미 사전 발급됨(DebateRestController).
- 브랜치 고정: refactor/#50-stomp-migration 에서만 작업. git checkout/switch/branch 금지.
- 커밋은 최대한 잘게(스토리당 여러 커밋). config/프로덕션/테스트, 클래스/관심사 단위로 분리. 각 커밋은 단독 컴파일 통과.
- 커밋 전 손댄 파일의 미사용 import 제거.
- 통합 테스트는 새 WS destination(@MessageMapping) 추가 스토리에만, 서비스 변경엔 항상 서비스 단위 테스트.
- 스토리 완료마다 ralph/migration_thinking.md 에 고민/트레이드오프/잠재위험/완화 기록.

---

## 2026-06-13 14:10 - US-001
- STOMP 메시지 브로커 설정 추가. WebSocketMessageBrokerConfigurer 구현 StompConfig를 controller/config에 신설.
  - @EnableWebSocketMessageBroker, registerStompEndpoints: addEndpoint("/ws").setAllowedOrigins(corsProperties.getOriginUrls())
  - configureMessageBroker: setApplicationDestinationPrefixes("/app"), enableSimpleBroker("/topic")
  - configureWebSocketTransport: setMessageSizeLimit(64*1024) — ~6400B 바이너리 오디오 청크 수용
- Files changed: app-debate/.../controller/config/StompConfig.java (신규)
- 검증: ./gradlew :app-debate:compileJava 통과, :app-debate:test BUILD SUCCESSFUL(컨텍스트에 SimpleBrokerMessageHandler 기동 확인 → 새 broker 빈이 기존 @SpringBootTest 컨텍스트와 충돌 없음)
- **Learnings for future iterations:**
  - CorsProperties는 CorsConfig의 @EnableConfigurationProperties(CorsProperties.class)로 이미 빈 등록됨 → @RequiredArgsConstructor로 바로 주입 가능. setAllowedOrigins는 String... 가변인자라 getOriginUrls()(String[])를 그대로 넘긴다.
  - 신설 STOMP endpoint는 "/ws", 기존 raw WS는 "/ws/stt" — 경로가 달라 마이그레이션 중 공존 충돌 없음. (BaseStompTest 템플릿 SOCKET_ENDPOINT도 "/ws")
  - 기존 raw WebSocketConfig는 @ConditionalOnProperty(stt.azure.enabled=true)로만 활성. StompConfig는 조건 없이 항상 활성(transport 교체 목표). US-007에서 raw config 제거 예정.
  - 어노테이션 길이순 정렬: @Configuration → @RequiredArgsConstructor → @EnableWebSocketMessageBroker.
---

## 2026-06-13 14:16 - US-002
- 서버→클라이언트 메시지를 /topic/debate/{debateId} 로 broadcast 하도록 전환.
  - WebSocketMessageSender: @RequiredArgsConstructor 로 SimpMessagingTemplate 주입, broadcast(String debateId, WebSocketMessage) 추가 → convertAndSend("/topic/debate/%s".formatted(debateId), message). 기존 send(DebateSession/WebSocketSession,...) 오버로드는 raw 핸들러·예외핸들러 컴파일 위해 유지(US-007에서 제거).
  - TranscribeEventListener: TRANSCRIPTION 발송을 broadcast(debateId,...) 로. sendTranscription 시그니처에서 DebateSession 제거(미사용 import 정리).
  - TranscribeRefiningService: REFINED_TRANSCRIPTION 발송을 broadcast(debateId,...) 로. session 파라미터는 debateId() 추출에 여전히 사용.
- Files changed: ws/sender/WebSocketMessageSender.java, event/TranscribeEventListener.java, service/transcript/TranscribeRefiningService.java, (test) ws/sender/WebSocketMessageSenderTest.java(신규), event/TranscribeEventListenerTest.java, service/transcript/TranscribeRefiningServiceTest.java
- 검증: ./gradlew :app-debate:compileJava 통과, :app-debate:test BUILD SUCCESSFUL(MySQL 13306/Redis 16379 로컬 기동 상태).
- 커밋 3개: (1) sender broadcast+test, (2) listener 전환+test, (3) refine 전환+test. 각각 단독 컴파일+테스트 통과.
- **Learnings for future iterations:**
  - SimpMessagingTemplate 빈은 US-001 StompConfig(@EnableWebSocketMessageBroker)가 자동 등록 → WebSocketMessageSender 가 조건 없이 주입 가능. @SpringBootTest 컨텍스트도 SimpleBrokerMessageHandler 기동되어 충돌 없음.
  - broadcast 시그니처는 String debateId 를 받는다(메시지 페이로드의 long debateId 와 별개). 토픽 경로 포맷은 "/topic/debate/{debateId}" — BaseStompTest 구독 경로와 일치시킬 것(US-004~006).
  - 단위 테스트 verify 전환 시: send(any(DebateSession.class),...) → broadcast(eq(debateId),...)/broadcast(any(),any()). ArgumentMatchers.eq static import 필요.
  - 커밋 seam: 프로덕션 행위 변경은 해당 단위 테스트와 같은 커밋으로 묶어야 중간 커밋도 :app-debate:test 그린 유지(테스트만 뒤로 미루면 중간 커밋이 깨짐).
  - 아직 raw 경로(SttWebSocketHandler·WebSocketExceptionHandler)는 send(...) 오버로드 사용 중 → broadcast 와 공존. US-007 제거 전까지 양 경로 동시 존재.
---

## 2026-06-13 14:26 - US-003
- 세션 lifecycle 을 debateId 키로 전환. DebateSession 에서 WebSocketSession 의존 완전 제거.
  - DebateSession: `record DebateSession(String debateId)` 로 축소. connection 필드, ATTR_DEBATE_ID 상수, WebSocketSession 생성자, 타입 레벨 Javadoc 모두 제거.
  - DebateStreamingService: handleControlMessage(ControlMessage)(session 파라미터 제거)·stopDebateWithRemainingRefine(String)·stopDebateIfActive(String)·startDebate(String) 로 시그니처 debateId 기반 정리. WebSocketSession import 제거.
  - SttWebSocketHandler: ATTR_DEBATE_ID 상수를 직접 보유(public static final). handleTextMessage 에서 control.isStart() 일 때 attribute 를 직접 set 하고(바이너리 핸들러가 읽을 수 있게), handleControlMessage(control) 호출 후 send(WebSocketSession,...) 직접 사용. afterConnectionClosed 는 attribute 에서 debateId 추출(Optional, 삼항 금지) 후 stopDebateIfActive(debateId).
  - WebSocketExceptionHandler: SttWebSocketHandler.ATTR_DEBATE_ID 참조로 attribute 에서 debateId 추출 + send(session,...) (US-003 직전 별도 커밋으로 attribute 추출로 선전환해 둠).
  - WebSocketMessageSender: 미사용 send(DebateSession,...) 오버로드 + DebateSession import 제거. broadcast/send(WebSocketSession,...) 유지.
- Files changed: domain/session/DebateSession.java, service/debate/DebateStreamingService.java, ws/handler/SttWebSocketHandler.java, exception/WebSocketExceptionHandler.java, ws/sender/WebSocketMessageSender.java, (test) DebateStreamingServiceTest·SttWebSocketHandlerTest·TranscribeRefiningSchedulerTest·AgendaAnalyzeSchedulerTest·DebateSessionDomainRepositoryTest·InMemoryDebateSessionRepositoryTest·TranscribeEventListenerTest·TranscribeRefiningServiceTest·AgendaAnalyzeServiceTest
- 검증: ./gradlew :app-debate:compileJava·compileTestJava 통과, :app-debate:test BUILD SUCCESSFUL(MySQL 13306/Redis 16379 로컬).
- 커밋 2개: (1) WebSocketExceptionHandler attribute 추출(독립·단독 그린), (2) DebateSession 축소+lifecycle 전환(prod+test 원자적).
- **Learnings for future iterations:**
  - DebateSession 의 shape 변경은 모든 소비처(prod+test)에 동시 파급 → 한 커밋으로 원자 처리해야 중간 커밋도 :app-debate:test 그린 유지(US-002 학습 재확인). 단, 의존 없는 선행 리팩터(WebSocketExceptionHandler 의 attribute 추출)는 현행 DebateSession 으로도 컴파일되어 별도 작은 커밋으로 분리 가능.
  - ATTR_DEBATE_ID 는 WS attribute 키라 순수 도메인 record(DebateSession)에 둘 자리가 아니다 → raw 전용 SttWebSocketHandler 로 이동. SttWebSocketHandler·WebSocketExceptionHandler 둘 다 raw 경로라 US-007 에서 함께 삭제될 때 상수도 같이 사라진다.
  - 서비스(TranscribeRefiningService·AgendaAnalyzeService)는 여전히 DebateSession 을 받지만 내부에서 session.debateId() 만 사용 → 시그니처 변경 불필요. 스케줄러는 findActiveSessions()(List<DebateSession>) 순회 그대로.
  - 핸들러 afterConnectionClosed/exceptionHandler 의 debateId 추출은 null 가능 → 삼항 금지 컨벤션에 따라 Optional.ofNullable(...).map(Object::toString).orElse(null) 또는 String.valueOf(...) 사용.
  - SttWebSocketHandlerTest: send 검증을 send(any(DebateSession.class),...) → send(eq(connection),...) 로 바꿔야 한다(핸들러가 WebSocketSession 오버로드 직접 호출).
---
